name = "mochilog-web"
# Set compatibility_date to a past date to avoid Cloudflare API "future date" validation error.
# If you deploy on a different day, update this to today's date (not in the future).
compatibility_date = "2025-12-23"
main = "src/index.tsx"

[observability]
enabled = true
